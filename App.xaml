<Application x:Class="BSG.Tools.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:BSG.Tools">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Swapped at runtime by ThemeManager; keep this as the first
                     merged dictionary so ThemeManager can replace it in place. -->
                <ResourceDictionary Source="Themes/LightTheme.xaml"/>
                <!-- Swapped at runtime by LocalizationManager; keep this as the
                     second merged dictionary so LocalizationManager can replace
                     it in place. -->
                <ResourceDictionary Source="Localization/Strings.vi.xaml"/>
            </ResourceDictionary.MergedDictionaries>

        <!-- ===================== DESIGN TOKENS (theme-independent) ===================== -->
        <!-- Sizes live here once; colors live in Themes/*.xaml so they swap with the theme. -->
        <CornerRadius x:Key="RadiusControl">8</CornerRadius>
        <!-- RadiusControl + focus ring thickness, so the ring hugs the control's rounded corners. -->
        <CornerRadius x:Key="RadiusFocusRing">11</CornerRadius>
        <CornerRadius x:Key="RadiusCard">12</CornerRadius>

        <Thickness x:Key="PaddingCard">22</Thickness>
        <Thickness x:Key="SpacingCard">0,0,0,20</Thickness>
        <Thickness x:Key="SpacingField">0,0,0,18</Thickness>
        <Thickness x:Key="SpacingLabel">0,0,0,6</Thickness>

        <system:Double x:Key="FontSizeLabel">12</system:Double>
        <system:Double x:Key="FontSizeBody">13</system:Double>
        <system:Double x:Key="FontSizeInput">14</system:Double>
        <system:Double x:Key="FontSizeSection">15</system:Double>

        <!-- ===================== WINDOW ===================== -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource BrushWindowBackground}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
        </Style>

        <!-- ===================== CARD ===================== -->
        <!-- Light theme: shadow only (BrushCardBorder is transparent). Dark theme: the shadow
             barely shows, so BrushCardBorder supplies a thin border instead. -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource BrushCardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BrushCardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{StaticResource RadiusCard}"/>
            <Setter Property="Padding" Value="{StaticResource PaddingCard}"/>
            <Setter Property="Margin" Value="{StaticResource SpacingCard}"/>
            <Setter Property="Effect" Value="{DynamicResource EffectCardShadow}"/>
        </Style>

        <!-- Transparent host around a button that can be disabled. A disabled element is skipped
             by mouse hit-testing, so its own Cursor never shows; the host underneath then gets
             the mouse and shows "not allowed". While the button is enabled it covers the host
             and its own hand cursor wins. -->
        <Style x:Key="DisabledCursorHostStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="No"/>
        </Style>

        <!-- ===================== TEXTBLOCK ===================== -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
            <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
        </Style>

        <!-- Label above an input: smaller and bolder than the value inside the input. -->
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="Foreground" Value="{DynamicResource BrushFieldLabel}"/>
            <Setter Property="FontSize" Value="{StaticResource FontSizeLabel}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="{StaticResource SpacingLabel}"/>
        </Style>

        <!-- Field label that dims together with the input it describes, for fields
             whose enabled state is toggled at runtime (e.g. supplier info). -->
        <Style x:Key="SupplierFieldLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource FieldLabelStyle}">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{DynamicResource BrushTextDisabled}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Leading title of a card/section, promoted above the regular field labels. -->
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="{StaticResource FontSizeSection}"/>
        </Style>

        <!-- Section title variant that also dims together with the checkbox it depends on. -->
        <Style x:Key="SupplierSectionTitleStyle" TargetType="TextBlock" BasedOn="{StaticResource SectionTitleStyle}">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{DynamicResource BrushTextDisabled}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ===================== TEXTBOX ===================== -->
        <!-- Focus is shown by the accent border plus a halo ("focusRing") drawn in a negative
             margin outside the control, so neither the control's size nor its neighbours move. -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource BrushCardBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="FontSize" Value="{StaticResource FontSizeInput}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="{DynamicResource BrushAccent}"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource BrushAccent}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="focusRing" Margin="-3" BorderThickness="3"
                                    BorderBrush="Transparent" CornerRadius="{StaticResource RadiusFocusRing}"
                                    IsHitTestVisible="False"/>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{StaticResource RadiusControl}">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                            <!-- "Unsaved change" bar: sits in the left padding, so it never covers text or resizes the box. -->
                            <Border x:Name="modifiedBar" Width="3" CornerRadius="1.5" Margin="4,7,0,7"
                                    HorizontalAlignment="Left" Background="{DynamicResource BrushModified}"
                                    IsHitTestVisible="False" Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushReadOnlyBackground}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                                <Setter TargetName="focusRing" Property="BorderBrush" Value="{DynamicResource BrushFocusGlow}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushDisabledBackground}"/>
                                <Setter Property="Foreground" Value="{DynamicResource BrushTextDisabled}"/>
                            </Trigger>
                            <Trigger Property="local:ModifiedState.IsModified" Value="True">
                                <Setter TargetName="modifiedBar" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Read-only path field (source file / output folder): value is only ever set
             via its picker button, so it shows a non-text cursor and, while empty, a
             placeholder hint instead of a blank box. -->
        <Style x:Key="ReadOnlyPathTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="focusRing" Margin="-3" BorderThickness="3"
                                    BorderBrush="Transparent" CornerRadius="{StaticResource RadiusFocusRing}"
                                    IsHitTestVisible="False"/>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{StaticResource RadiusControl}">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                            <!-- FontSize set locally: the app-wide implicit TextBlock style would
                                 otherwise force the body size instead of the input size. The extra
                                 2px left padding matches TextBox's built-in caret gutter, so the hint
                                 lines up with typed text. (TextBox applies Padding to its content
                                 itself, so PART_ContentHost must not repeat it as a Margin.) -->
                            <TextBlock x:Name="placeholder"
                                       Text="{TemplateBinding Tag}"
                                       Foreground="{DynamicResource BrushTextSecondary}"
                                       FontSize="{TemplateBinding FontSize}"
                                       Margin="{TemplateBinding Padding}"
                                       Padding="2,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushReadOnlyBackground}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                                <Setter TargetName="focusRing" Property="BorderBrush" Value="{DynamicResource BrushFocusGlow}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushDisabledBackground}"/>
                                <Setter Property="Foreground" Value="{DynamicResource BrushTextDisabled}"/>
                            </Trigger>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== CHECKBOX ===================== -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
            <Setter Property="Padding" Value="10,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Grid VerticalAlignment="Center">
                                <Border x:Name="focusRing" Margin="-3" BorderThickness="3" CornerRadius="8"
                                        BorderBrush="Transparent" IsHitTestVisible="False"/>
                                <Border x:Name="box" Width="18" Height="18" CornerRadius="5"
                                        BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1.5"
                                        Background="{DynamicResource BrushCardBackground}">
                                    <Path x:Name="checkMark" Data="M2,7 L6,11 L14,2"
                                          Stroke="{DynamicResource BrushOnAccent}" StrokeThickness="2"
                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                          Visibility="Collapsed"/>
                                </Border>
                            </Grid>
                            <!-- Explicit ContentTemplate so the label follows this CheckBox's Foreground
                                 (e.g. dims when disabled) instead of the app-wide implicit TextBlock style. -->
                            <ContentPresenter Margin="{TemplateBinding Padding}" VerticalAlignment="Center">
                                <ContentPresenter.ContentTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=CheckBox}}"/>
                                    </DataTemplate>
                                </ContentPresenter.ContentTemplate>
                            </ContentPresenter>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="box" Property="Background" Value="{DynamicResource BrushAccent}"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="focusRing" Property="BorderBrush" Value="{DynamicResource BrushFocusGlow}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource BrushTextDisabled}"/>
                                <Setter TargetName="box" Property="Background" Value="{DynamicResource BrushDisabledBackground}"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== BUTTON (secondary / default: outline) ===================== -->
        <!-- Every button without an explicit style is a secondary action, drawn as an accent
             outline on a transparent background so it never looks like an input field. -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource BrushAccent}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource RadiusControl}">
                            <!-- A plain-string Content (e.g. Content="Lưu cài đặt") gets auto-wrapped by
                                 WPF's default ContentPresenter in a TextBlock that, empirically, does NOT
                                 resolve implicit TextBlock styles through this Border's (or any ancestor's)
                                 Resources — it always resolves to the app-wide implicit TextBlock style
                                 instead, ignoring this button's own Foreground. Giving ContentPresenter an
                                 explicit ContentTemplate and setting Foreground there as a literal element
                                 value (not a Style Setter) sidesteps that entirely, since a local value
                                 always wins over any Style Setter. -->
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                               HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                               VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <ContentPresenter.ContentTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                    </DataTemplate>
                                </ContentPresenter.ContentTemplate>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushAccentSubtle}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushAccentSubtlePressed}"/>
                            </Trigger>
                            <!-- Disabled keeps the button's own shape and colors, just faded, so it
                                 still reads as a button rather than an empty input. -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== BUTTON (accent / primary action) ===================== -->
        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource BrushAccent}"/>
            <Setter Property="Foreground" Value="{DynamicResource BrushOnAccent}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource RadiusControl}">
                            <!-- Same ContentTemplate fix as the base Button style (see comment there). -->
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                               HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                               VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <ContentPresenter.ContentTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                    </DataTemplate>
                                </ContentPresenter.ContentTemplate>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushAccentHover}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BrushAccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushAccentPressed}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BrushAccentPressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== BUTTON (link / low-emphasis auxiliary action) ===================== -->
        <!-- For actions like "Tải file mẫu" that sit next to a primary action (e.g. "Chọn
             file...") but are used far less often — styled as a text link, clearly distinct
             so it isn't mistaken for the primary button next to it. -->
        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource BrushAccent}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <!-- Same reasoning as the other button styles: a plain-string Content's
                                 auto-wrapped TextBlock ignores this button's own Foreground unless set
                                 as a literal element value via an explicit ContentTemplate. -->
                            <ContentPresenter VerticalAlignment="Center">
                                <ContentPresenter.ContentTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" TextDecorations="Underline"
                                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                    </DataTemplate>
                                </ContentPresenter.ContentTemplate>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource BrushAccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource BrushAccentPressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== COMBOBOX ===================== -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border" Background="Transparent" Padding="{TemplateBinding Padding}"
                                CornerRadius="6">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushAccentSubtle}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushAccentSubtlePressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource BrushCardBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="FontSize" Value="{StaticResource FontSizeInput}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="focusRing" Margin="-3" BorderThickness="3"
                                    BorderBrush="Transparent" CornerRadius="{StaticResource RadiusFocusRing}"
                                    IsHitTestVisible="False"/>
                            <!-- Visible chrome: one border spanning the whole control, drawn
                                 once here rather than inside the toggle button, so it never
                                 splits into a separate box around just the arrow. -->
                            <Border x:Name="border"
                                    Background="{DynamicResource BrushCardBackground}"
                                    BorderBrush="{DynamicResource BrushBorder}"
                                    BorderThickness="1" CornerRadius="{StaticResource RadiusControl}">
                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <!-- Explicit TextBlock so the selected text uses the input size,
                                         not the app-wide implicit TextBlock style's body size. -->
                                    <TextBlock Grid.Column="0" IsHitTestVisible="False"
                                               Text="{Binding SelectionBoxItem, RelativeSource={RelativeSource TemplatedParent}}"
                                               FontSize="{TemplateBinding FontSize}"
                                               HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Stroke="{DynamicResource BrushTextSecondary}"
                                          StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </Grid>
                            </Border>

                            <!-- Transparent hit-test layer on top: opens the dropdown without
                                 drawing any chrome of its own. -->
                            <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup x:Name="Popup" Placement="Bottom" AllowsTransparency="True"
                                   Focusable="False" PopupAnimation="Slide"
                                   IsOpen="{TemplateBinding IsDropDownOpen}">
                                <Border Background="{DynamicResource BrushCardBackground}"
                                        BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1"
                                        CornerRadius="{StaticResource RadiusControl}" MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="200" Margin="0,4,0,0" Padding="4">
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                                <Setter TargetName="focusRing" Property="BorderBrush" Value="{DynamicResource BrushFocusGlow}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                                <Setter TargetName="focusRing" Property="BorderBrush" Value="{DynamicResource BrushFocusGlow}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== SCROLLBAR ===================== -->
        <Style x:Key="ThinScrollBarThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="thumbBorder" Background="{DynamicResource BrushBorder}" CornerRadius="3"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="thumbBorder" Property="Background" Value="{DynamicResource BrushTextSecondary}"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="thumbBorder" Property="Background" Value="{DynamicResource BrushAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="MinWidth" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="True" IsEnabled="{TemplateBinding IsEnabled}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ThinScrollBarThumb}" Width="6" Margin="1,0"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="8"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="MinHeight" Value="8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid Background="{TemplateBinding Background}">
                                    <Track x:Name="PART_Track" IsEnabled="{TemplateBinding IsEnabled}">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Command="ScrollBar.PageLeftCommand" Opacity="0" Focusable="False"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Command="ScrollBar.PageRightCommand" Opacity="0" Focusable="False"/>
                                        </Track.IncreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ThinScrollBarThumb}" Height="6" Margin="0,1"/>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ===================== TABCONTROL / TABITEM ===================== -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="0,0,0,1">
                                <TabPanel IsItemsHost="True" Margin="0,0,0,-1"/>
                            </Border>
                            <!-- The PART_ name lets TabItem's automation peer expose the selected
                                 tab's controls to UI Automation (screen readers, UI tests). -->
                            <ContentPresenter x:Name="PART_SelectedContentHost" Grid.Row="1" ContentSource="SelectedContent"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource BrushTextSecondary}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid Background="Transparent" Margin="0,0,4,0">
                            <!-- Explicit ContentTemplate so the header follows this TabItem's Foreground
                                 (secondary / hover / accent) instead of the app-wide implicit TextBlock style. -->
                            <ContentPresenter x:Name="content" ContentSource="Header"
                                               Margin="{TemplateBinding Padding}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter.ContentTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TabItem}}"
                                                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=TabItem}}"/>
                                    </DataTemplate>
                                </ContentPresenter.ContentTemplate>
                            </ContentPresenter>
                            <Rectangle x:Name="underline" Height="3" RadiusX="1.5" RadiusY="1.5"
                                       VerticalAlignment="Bottom" Margin="10,0" Fill="Transparent"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="underline" Property="Fill" Value="{DynamicResource BrushAccent}"/>
                                <Setter Property="Foreground" Value="{DynamicResource BrushAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
